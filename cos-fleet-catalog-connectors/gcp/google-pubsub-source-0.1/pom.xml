<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <parent>
        <groupId>org.bf2</groupId>
        <artifactId>cos-fleet-catalog-connectors-gcp</artifactId>
        <version>999-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>google-pubsub-source-0.1</artifactId>
    <version>0.1</version>
    <name>Google Cloud Pubsub Source</name>
    <description>Google Cloud Pubsub Source</description>

    <packaging>jar</packaging>

    <properties>
        <quarkus.container-image.name>cos-connector-google-pubsub-source</quarkus.container-image.name>
        <quarkus.container-image.tag>${project.version}.${cos.connector.revision}</quarkus.container-image.tag>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.bf2</groupId>
                <artifactId>camel-connector-catalog-maven-plugin</artifactId>
                <configuration>
                    <connectors>
                        <connector>
                            <adapter>
                                <prefix>gcp</prefix>
                                <name>google-pubsub-source</name>
                                <version>${camel-kamelets-catalog.version}</version>
                            </adapter>
                            <kafka>
                                <prefix>kafka</prefix>
                                <name>cos-kafka-sink</name>
                                <version>${cos.kamelets.version}</version>
                            </kafka>
                        </connector>
                    </connectors>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
